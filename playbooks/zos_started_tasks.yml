---
- name: Gather all z/OS STCs
  hosts: zos
  gather_facts: false
  environment: "{{ environment_vars }}"
  
  tasks:
  - name: Display all started tasks.
    ibm.ibm_zos_core.zos_started_task:
      state: "displayed"
      job_name: "all"